- name: Install golang
  community.general.snap:
    name:
      - go
    classic: true
- name: Install recordgetter
  ansible.builtin.command: go install github.com/brotherlogic/{item}}/{item}}_cli@latest
  loop:
    - recordgetter
    - recordcleaner
    - recordcollection
